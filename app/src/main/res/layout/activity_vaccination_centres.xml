<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.VaccinationCentresActivity"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <EditText
            android:id="@+id/idEdtPinCode"
            android:layout_width="0dp"
            android:layout_height="@dimen/touchAreaSmall"
            android:layout_weight="1"
            android:layout_margin="@dimen/marginLarge"
            android:hint="Enter PinCode"
            android:maxLength="6"
            android:inputType="number"
            android:background="@drawable/edit_primary"/>

        <!--button for searchng the data-->
        <Button
            android:id="@+id/idBtnSearch"
            android:layout_width="@dimen/touchAreaLarge"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/marginLarge"
            android:background="@drawable/btn_primary"
            android:text="Search"
            android:textAllCaps="false" />
    </LinearLayout>

    <!--progress bar for loading indicator-->
    <ProgressBar
        android:id="@+id/idPBLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone" />

    <!--recycler view for displaying results in the form of list-->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/centersRV"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/layout_vaccination_centre" />
</LinearLayout>